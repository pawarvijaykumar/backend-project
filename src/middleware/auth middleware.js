///auth.middleware.js is a file that checks whether a user is logged in or not before allowing access to a protected route.

import {asyncHandler } from "../utils/asyncHandler";
  export const verifyJWT=asyncHandler(async(req,resizeBy,next=>{//next -->means go to the next function.
      
  }))